# Required: use long random strings in production (e.g. openssl rand -hex 24)
SECRET=your-signing-secret-change-me
ADMIN_SECRET=your-admin-secret-change-me

# For Vercel + Upstash (Option A): create DB at upstash.com, then set:
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token

# For Option B (Node server): optional – clients enabled on startup (comma-separated)
ENABLED_CLIENTS=dev-client
# DATA_FILE=./enabled-clients.json
# PORT=3333
